// Enum for maintenance types
enum MaintenanceType {
  regularService,
  oilChange,
  tireRotation,
  brakeInspection,
  other,
}

// Enum for fuel types
enum FuelType {
  petrol,
  diesel,
  electric,
  hybrid,
  other,
}

// Enum for repair categories
enum RepairCategory {
  engine,
  transmission,
  suspension,
  brakeSystem,
  electrical,
  other,
}

// Enum for user roles
enum UserRole {
  user,
  admin,
}